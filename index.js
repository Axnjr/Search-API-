import React from 'react';
import ReactDOM from 'react-dom/client';
import GoogleAssistant from './GoogleAssistant.js';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode> 
    <GoogleAssistant/>    
  </React.StrictMode>
);
